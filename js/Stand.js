// same as the block class only difference is - apply isStatic: true in options
class Stand{
    constructor(x, y, width, height) {
        var options = {
           // set static property to true
        }
       
      }
     
}